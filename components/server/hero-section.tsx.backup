import Image from "next/image";
import { EVENT_DETAILS } from "@/lib/constants";

export function HeroSection() {
  return (
    <section className="relative w-full">
      {/* Hero Image Container */}
      <div className="relative w-full h-[400px] md:h-[500px] overflow-hidden">
        <Image
          src="/images/hero-teddy-bear.jpeg"
          alt={`${EVENT_DETAILS.title} untuk ${EVENT_DETAILS.babyName}`}
          fill
          priority
          className="object-cover object-center"
          sizes="100vw"
        />

        {/* Overlay Gradient */}
        <div className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-cream/90" />

        {/* Text Overlay */}
        <div className="absolute inset-0 flex flex-col items-center justify-end pb-12 px-4">
          <div className="text-center space-y-3 z-10">
            <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-baby-blue-dark drop-shadow-lg">
              {EVENT_DETAILS.title}
            </h1>
            <p className="text-2xl md:text-3xl font-semibold text-baby-blue-dark drop-shadow-md">
              {EVENT_DETAILS.babyName}
            </p>
            <div className="pt-2 space-y-1">
              <p className="text-lg md:text-xl text-baby-blue-dark drop-shadow-md">
                {EVENT_DETAILS.father}
              </p>
              <p className="text-lg md:text-xl text-baby-blue-dark drop-shadow-md">
                {EVENT_DETAILS.mother}
              </p>
            </div>
          </div>
        </div>
      </div>

      {/* Islamic Decorative Border */}
      <div className="w-full h-2 bg-gradient-to-r from-transparent via-gold to-transparent" />
    </section>
  );
}
